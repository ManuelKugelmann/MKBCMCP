{
  "name": "mkbc-mcp",
  "version": "0.1.0",
  "type": "module",
  "description": "Custom MCP server for project bootstrapping, regex grep, and local file storage",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "fastmcp": "^0.1.0",
    "octokit": "^4.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  }
}
